{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.oktopeak/clio-mcp",
  "description": "Connect Claude to Clio with 26 tools for matters, contacts, tasks, billing, users, audit-log export, and more. Supports stdio and HTTP/SSE transports.",
  "repository": {
    "url": "https://github.com/oktopeak/clio-mcp",
    "source": "github"
  },
  "version": "2.0.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@oktopeak/clio-mcp",
      "version": "2.0.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "CLIO_CLIENT_ID",
          "description": "Client ID from your Clio developer application (Settings \u2192 Developer Applications)",
          "isRequired": true,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "CLIO_CLIENT_SECRET",
          "description": "Client Secret from your Clio developer application",
          "isRequired": true,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "ENCRYPTION_KEY",
          "description": "Optional. Leave unset to auto-generate a key stored in the OS keychain (or ~/.clio-mcp/key.hex as fallback on headless Linux/WSL2). If set, must be a 64-character hex string — it will be migrated to the OS keychain on first use.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "CLIO_REDIRECT_PORT",
          "description": "Local port for the OAuth callback listener (default: 5678). Change if 5678 is in use.",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "CLIO_REGION",
          "description": "Clio region: 'us' (default) or 'eu'. Controls API and auth base URLs.",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "CLIO_API_BASE",
          "description": "Override the Clio API base URL (e.g. https://eu.app.clio.com/api/v4). Use instead of CLIO_REGION for fine-grained control.",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "CLIO_AUTH_URL",
          "description": "Override the OAuth authorization endpoint (e.g. https://eu.app.clio.com/oauth/authorize).",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "CLIO_TOKEN_URL",
          "description": "Override the OAuth token endpoint (e.g. https://eu.app.clio.com/oauth/token).",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        }
      ]
    },
    {
      "registryType": "npm",
      "identifier": "@oktopeak/clio-mcp",
      "version": "2.0.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "TOKEN_BROKER_URL",
          "description": "URL of Oktopeak's hosted token broker for the Clio App Directory listed install. Do not set CLIO_CLIENT_ID/CLIO_CLIENT_SECRET alongside this — the shared app's secret lives only on the broker, never on this machine.",
          "isRequired": true,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "ENCRYPTION_KEY",
          "description": "Optional. Leave unset to auto-generate a key stored in the OS keychain (or ~/.clio-mcp/key.hex as fallback on headless Linux/WSL2). If set, must be a 64-character hex string — it will be migrated to the OS keychain on first use.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        }
      ]
    }
  ]
}